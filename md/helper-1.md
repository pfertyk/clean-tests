```python
def _validate_tickets(self, is_normal_user=True):
    # login as self.user or self.admin if not is_normal_user
    # prepare complicated test data for tickets
    # call the API to create tickets
    # assert that ticket creation was successful
    # if so, return created tickets as a list
    # if not, return None
```
